# Configuration

The following table describe available configuration values.

| Variable                                  | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                         |
|:------------------------------------------|:--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `AUTH_MODE`                               | The auth mode. Possible values are `basic_single` and `basic_credentials`. Defaults to `basic_single`.                                                                                                                                                                                                                                                                                                                          |
| `BASIC_AUTH_CREDENTIALS`                  | For auth mode `basic_credentials`: list of semicolon separated credentials, e.g. `username1\|password1;username2\|password2`. Not set by default, you need to explicitly set it.                                                                                                                                                                                                                                                |
| `BASIC_AUTH_PASSWORD`                     | For auth mode `basic_single`: User's password for login. The user's password                                                                                                                                                                                                                                                                                                                                                    |
| `BASIC_AUTH_USER`                         | For auth mode `basic_single`: Username for login. The user name                                                                                                                                                                                                                                                                                                                                                                 |
| `CORS_ALLOW_CREDENTIALS`                  | CORS configuration. Defaults to `true`                                                                                                                                                                                                                                                                                                                                                                                          |
| `CORS_ALLOW_HEADERS`                      | CORS configuration. Defaults to `Authorization, Content-Type`                                                                                                                                                                                                                                                                                                                                                                   |
| `CORS_ALLOW_METHODS`                      | CORS configuration. Defaults to `GET, POST, PUT, PATCH, DELETE, OPTIONS`                                                                                                                                                                                                                                                                                                                                                        |
| `CORS_ALLOW_ORIGINS`                      | CORS configuration. Defaults to `*`                                                                                                                                                                                                                                                                                                                                                                                             |
| `CORS_EXPOSE_HEADERS`                     | CORS configuration. Defaults to `*`                                                                                                                                                                                                                                                                                                                                                                                             |
| `DB_MIGRATION_ENABLED`                    | See [database migration](#database-migration) for more detailed information. If `true` database migrations are executed; useful to disable when multiple instances are spawned. Defaults to `true`, possible values are `true` or `false`                                                                                                                                                                                       |
| `DB_POSTGRES_HOST`                        | The postgres host. Postgres host address, defaults to `localhost`                                                                                                                                                                                                                                                                                                                                                               |
| `DB_POSTGRES_NAME`                        | The postgres database name. Postgres database name                                                                                                                                                                                                                                                                                                                                                                              |
| `DB_POSTGRES_PASSWORD`                    | The postgres password. Postgres user password                                                                                                                                                                                                                                                                                                                                                                                   |
| `DB_POSTGRES_PORT`                        | The postgres port. Postgres port, defaults to `5432`                                                                                                                                                                                                                                                                                                                                                                            |
| `DB_POSTGRES_TZ`                          | The postgres time zone. Postgres time zone settings, defaults to `Etc/UTC`                                                                                                                                                                                                                                                                                                                                                      |
| `DB_POSTGRES_USER`                        | The postgres user. Postgres user name                                                                                                                                                                                                                                                                                                                                                                                           |
| `DB_TYPE`                                 | The database type (Postgres is **recommended**). Defaults to `postgres`, possible values are `postgres`                                                                                                                                                                                                                                                                                                                         |
| `LOCK_REDIS_ENABLED`                      | If locking via REDIS (multiple instances) is enabled. Requires REDIS. Otherwise uses in-memory locks. Defaults to `false`                                                                                                                                                                                                                                                                                                       |
| `LOCK_REDIS_HOST`                         | If locking via REDIS is enabled, this should point to a resolvable FQDN the REDIS instance runs on. Defaults to `localhost`                                                                                                                                                                                                                                                                                                     |
| `LOCK_REDIS_PORT`                         | If locking via REDIS is enabled, this should point to a resolvable port the REDIS instance runs on. Defaults to `6379`                                                                                                                                                                                                                                                                                                          |
| `LOCK_REDIS_DB_NAME`                      | If locking via REDIS is enabled, this should be a valid REDIS database name (a number). Defaults to `0`                                                                                                                                                                                                                                                                                                                         |
| `LOCK_REDIS_USERNAME`                     | If locking via REDIS is enabled, set credentials to connect to the REDIS instance. Make sure that both username and password environment variables are provided at the same time.                                                                                                                                                                                                                                               |                                                                                                                                                                                                                                                                                                                |
| `LOCK_REDIS_PASSWORD`                     | If locking via REDIS is enabled, set credentials to connect to the REDIS instance. Make sure that both username and password environment variables are provided at the same time.                                                                                                                                                                                                                                               |                                                                                                                                                                                                                                                                                                                |
| `LOCK_REDIS_TASK_LOCK_TRIES`              | If locking via REDIS is enabled, determines the maximum retries a single background task should try and wait for it's execution. When it cannot acquire the lock within the given tries value, it's skipped and waits until the next execution. Defaults to `1`, must be with a positive number                                                                                                                                 |
| `LOCK_REDIS_TASK_LOCK_AT_MOST`            | If locking via REDIS is enabled, determines the value how long the lock for any task is held at most. If a task's lock is not released in case the application crashes, this is the maximum duration a task needs to wait until next execution. It should cover the maximum required time for any task. Defaults to `30s` (30 seconds), qualifier can be `s = second`, `m = minute`, `h = hour` prefixed with a positive number |
| `LOCK_REDIS_TASK_RETRY_DELAY`             | If locking via REDIS is enabled, determines the value in which interval a task's lock is retried to be acquired if already locked. Defaults to `5s` (5 seconds), qualifier can be `s = second`, `m = minute`, `h = hour` prefixed with a positive number                                                                                                                                                                        |
| `LOGGING_DIRECTORY`                       | Logging directory. When set, logs will be added to a file called `upda.log` in addition to the standard output. Ensure that upda has access permissions. Use an external program for log rotation if desired.                                                                                                                                                                                                                   |
| `LOGGING_ENCODING`                        | Logging encoding. Possible are `console` and `json`. Defaults to `json`                                                                                                                                                                                                                                                                                                                                                         |
| `LOGGING_ENCODING_CALLER_ENCODER`         | Logging encoder for caller field. Possible are `short`, `full`. Defaults to `short`                                                                                                                                                                                                                                                                                                                                             |
| `LOGGING_ENCODING_DURATION_ENCODER`       | Logging encoder for duration fields. Possible are `seconds`, `nanos`, `millis`, `string`. Defaults to `seconds`                                                                                                                                                                                                                                                                                                                 |
| `LOGGING_ENCODING_LEVEL_ENCODER`          | Logging encoder for level field. Possible are `lowercase`, `lowercasecolor`, `capital`, `capitalcolor`. Defaults to `lowercase`                                                                                                                                                                                                                                                                                                 |
| `LOGGING_ENCODING_LEVEL_KEY`              | Name for level field, defaults to `level`                                                                                                                                                                                                                                                                                                                                                                                       |
| `LOGGING_ENCODING_MESSAGE_KEY`            | Name for message field, defaults to `msg`                                                                                                                                                                                                                                                                                                                                                                                       |
| `LOGGING_ENCODING_STACKTRACE_KEY`         | Name for stacktrace field, defaults to `stacktrace`                                                                                                                                                                                                                                                                                                                                                                             |
| `LOGGING_ENCODING_TIME_ENCODER`           | Logging encoder for time field. Possible are `epoch`, `epochmillis`, `epochnanos`, `iso8601`, `rfc3339`, `rfc3339nano`. Defaults to `rfc3339`                                                                                                                                                                                                                                                                                   |
| `LOGGING_ENCODING_TIME_KEY`               | Name for time field, defaults to `ts`                                                                                                                                                                                                                                                                                                                                                                                           |
| `LOGGING_LEVEL`                           | Logging level. Possible are `debug`, `info`, `warn`, `error`, `dpanic`, `panic`, `fatal`. Setting to `debug` enables high verbosity output. Defaults to `info`                                                                                                                                                                                                                                                                  |
| `LOGGING_UTC`                             | Impacts calculation on duration for route logging (time taken), defaults to `false`                                                                                                                                                                                                                                                                                                                                             |
| `PROMETHEUS_ENABLED`                      | If Prometheus metrics are exposed. Defaults to `false`                                                                                                                                                                                                                                                                                                                                                                          |
| `PROMETHEUS_PORT`                         | Port. Defaults to `8080` (same as `SERVER_PORT`). If it differs from `SERVER_PORT`, a separate Prometheus server is started on that port. Please also see the listen and base path environment variables in addition.                                                                                                                                                                                                           |
| `PROMETHEUS_LISTEN`                       | Prometheus's listen address . Defaults to empty which equals `0.0.0.0`                                                                                                                                                                                                                                                                                                                                                          |
| `PROMETHEUS_BASE_PATH`                    | Prometheus's base path. Defaults to `/`, must always end with trailing slash                                                                                                                                                                                                                                                                                                                                                    |
| `PROMETHEUS_METRICS_PATH`                 | Defines the metrics endpoint path. Defaults to `/metrics` (adheres to `PROMETHEUS_BASE_PATH`)                                                                                                                                                                                                                                                                                                                                   |
| `PROMETHEUS_SECURE_TOKEN_ENABLED`         | If Prometheus metrics endpoint is protected by a token when enabled (**recommended**). Defaults to `true`                                                                                                                                                                                                                                                                                                                       |
| `PROMETHEUS_SECURE_TOKEN`                 | The token securing the metrics endpoint when enabled (**recommended**)                                                                                                                                                                                                                                                                                                                                                          |
| `SECRET`                                  | A 32 character long secure random secret used for encrypting some data inside the database. When data has been created inside the database, the secret cannot be changed anymore, otherwise decryption fails . Not set, generate securely, e.g., with `openssl rand -hex 16`.                                                                                                                                                   |
| `SERVER_BASE_PATH`                        | Server's base path. Defaults to `/`, must always end with trailing slash                                                                                                                                                                                                                                                                                                                                                        |
| `SERVER_LISTEN`                           | Server's listen address. Defaults to empty which equals `0.0.0.0`                                                                                                                                                                                                                                                                                                                                                               |
| `SERVER_PORT`                             | Port. Defaults to `8080`                                                                                                                                                                                                                                                                                                                                                                                                        |
| `SERVER_TIMEOUT`                          | Timeout the server waits before shutting down to end any pending tasks. Defaults to `10s` (10 second), qualifier can be `s = second`, `m = minute`, `h = hour` prefixed with a positive number                                                                                                                                                                                                                                  |
| `SERVER_TLS_CERT_PATH`                    | When TLS enabled, provide the certificate path                                                                                                                                                                                                                                                                                                                                                                                  |
| `SERVER_TLS_ENABLED`                      | If server uses TLS. Defaults `false`                                                                                                                                                                                                                                                                                                                                                                                            |
| `SERVER_TLS_KEY_PATH`                     | When TLS enabled, provide the key path                                                                                                                                                                                                                                                                                                                                                                                          |
| `TASK_ACTIONS_CLEAN_STALE_ENABLED`        | If background task should run to do housekeeping of stale (handled, meaning success or error state) actions from the database. Defaults to `true`                                                                                                                                                                                                                                                                               |
| `TASK_ACTIONS_CLEAN_STALE_INTERVAL`       | Interval at which a background task does housekeeping by deleting stale (handled) actions from the database. Defaults to `12h` (12 hours), qualifier can be `s = second`, `m = minute`, `h = hour` prefixed with a positive number                                                                                                                                                                                              |
| `TASK_ACTIONS_CLEAN_STALE_MAX_AGE`        | Number defining at which age stale (handled) actions are deleted by the background task (_updatedAt_ attribute decides). Defaults to `720h` (720 hours = 30 days), qualifier can be `s = second`, `m = minute`, `h = hour` prefixed with a positive number                                                                                                                                                                      |
| `TASK_ACTIONS_ENQUEUE_BATCH_SIZE`         | Number defining how many unhandled events are processed in a batch by the background task. Defaults to `1`, must be positive number                                                                                                                                                                                                                                                                                             |
| `TASK_ACTIONS_ENQUEUE_ENABLED`            | If background task should run to enqueue matching actions derived from events (actions are invocation separately after being enqueued). Defaults to `true`                                                                                                                                                                                                                                                                      |
| `TASK_ACTIONS_ENQUEUE_INTERVAL`           | Interval at which a background task does check to enqueue actions. Defaults to `10s` (10 seconds), qualifier can be `s = second`, `m = minute`, `h = hour` prefixed with a positive number                                                                                                                                                                                                                                      |
| `TASK_ACTIONS_INVOKE_BATCH_SIZE`          | Number defining how many enqueued actions are processed in a batch by the background task. Defaults to `1`, must be positive number                                                                                                                                                                                                                                                                                             |
| `TASK_ACTIONS_INVOKE_ENABLED`             | If background task should run to invoke enqueued actions derived. Defaults to `true`                                                                                                                                                                                                                                                                                                                                            |
| `TASK_ACTIONS_INVOKE_INTERVAL`            | Interval at which a background task does check to invoke enqueued actions. Defaults to `10s` (10 seconds), qualifier can be `s = second`, `m = minute`, `h = hour` prefixed with a positive number                                                                                                                                                                                                                              |
| `TASK_ACTIONS_INVOKE_MAX_RETRIES`         | Number defining how often actions are invoked in case of an error, if exceeded, those actions are not retried again. Defaults to `3`, must be positive number                                                                                                                                                                                                                                                                   |
| `TASK_EVENT_CLEAN_STALE_ENABLED`          | If background task should run to do housekeeping of stale (old) events from the database. Defaults to `false`                                                                                                                                                                                                                                                                                                                   |
| `TASK_EVENT_CLEAN_STALE_INTERVAL`         | Interval at which a background task does housekeeping by deleting stale (old) events from the database. Defaults to `8h` (8 hours), qualifier can be `s = second`, `m = minute`, `h = hour` prefixed with a positive number                                                                                                                                                                                                     |
| `TASK_EVENT_CLEAN_STALE_MAX_AGE`          | Number defining at which age stale (old) events are deleted by the background task (_updatedAt_ attribute decides). Defaults to `2190h` (2190 hours = 3 months), qualifier can be `s = second`, `m = minute`, `h = hour` prefixed with a positive number                                                                                                                                                                        |
| `TASK_PROMETHEUS_REFRESH_INTERVAL`        | Interval at which a background task updates custom metrics. Defaults to `60s` (60 seconds), qualifier can be `s = second`, `m = minute`, `h = hour` prefixed with a positive number                                                                                                                                                                                                                                             |
| `TASK_UPDATE_CLEAN_STALE_ENABLED`         | If background task should run to do housekeeping of stale (ignored/approved) updates from the database. Defaults to `false`                                                                                                                                                                                                                                                                                                     |
| `TASK_UPDATE_CLEAN_STALE_INTERVAL`        | Interval at which a background task does housekeeping by deleting stale (ignored/approved) updates from the database. Defaults to `1h` (1 hour), qualifier can be `s = second`, `m = minute`, `h = hour` prefixed with a positive number                                                                                                                                                                                        |
| `TASK_UPDATE_CLEAN_STALE_MAX_AGE`         | Number defining at which age stale (ignored/approved) updates are deleted by the background task (_updatedAt_ attribute decides). Defaults to `720h` (168 hours = 1 week), qualifier can be `s = second`, `m = minute`, `h = hour` prefixed with a positive number                                                                                                                                                              |
| `TZ`                                      | The time zone (**recommended** to set it properly, background tasks depend on it). Defaults to `Etc/UTC`, can be any time zone according to _tz database_.                                                                                                                                                                                                                                                                      |
| `WEB_INTERFACE_API_URL`                   | If web interface enabled, full API URL including trailing slash, e.g. `https://upda.domain.tld/api/v1/`.                                                                                                                                                                                                                                                                                                                        |
| `WEB_INTERFACE_CC_ENABLED`                | If cache control should be enabled for the web interface, allowing to specify other values. Defaults to `true`. If disabled, the _no cache_ directive is provided.                                                                                                                                                                                                                                                              |
| `WEB_INTERFACE_CC_IMMUTABLE`              | When cache control for the web interface is enabled, controls this [cache-control header attribute](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control). Defaults to `false`.                                                                                                                                                                                                                    |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `WEB_INTERFACE_CC_MAX_AGE`                | When cache control for the web interface is enabled, controls this [cache-control header attribute](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control). Defaults to `24h`. Can be a duration like `1h` (one hour). Qualifier can be `s = second`, `m = minute`, `h = hour` prefixed with a positive number.                                                                                     |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       
| `WEB_INTERFACE_CC_MUST_REVALIDATE`        | When cache control for the web interface is enabled, controls this [cache-control header attribute](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control). Defaults to `true`.                                                                                                                                                                                                                     |
| `WEB_INTERFACE_CC_NO_CACHE`               | When cache control for the web interface is enabled, controls this [cache-control header attribute](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control). Defaults to `false`.                                                                                                                                                                                                                    |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `WEB_INTERFACE_CC_NO_STORE`               | When cache control for the web interface is enabled, controls this [cache-control header attribute](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control). Defaults to `false`.                                                                                                                                                                                                                    |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `WEB_INTERFACE_CC_NO_TRANSFORM`           | When cache control for the web interface is enabled, controls this [cache-control header attribute](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control). Defaults to `false`.                                                                                                                                                                                                                    |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `WEB_INTERFACE_CC_PRIVATE`                | When cache control for the web interface is enabled, controls this [cache-control header attribute](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control). Defaults to `false`.                                                                                                                                                                                                                    |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `WEB_INTERFACE_CC_PROXY_REVALIDATE`       | When cache control for the web interface is enabled, controls this [cache-control header attribute](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control). Defaults to `true`.                                                                                                                                                                                                                     |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `WEB_INTERFACE_CC_PUBLIC`                 | When cache control for the web interface is enabled, controls this [cache-control header attribute](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control). Defaults to `true`.                                                                                                                                                                                                                     |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `WEB_INTERFACE_CC_SMAX_AGE`               | When cache control for the web interface is enabled, controls this [cache-control header attribute](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control). Not set by default. Can be a duration like `1h` (one hour). Qualifier can be `s = second`, `m = minute`, `h = hour` prefixed with a positive number.                                                                                    |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `WEB_INTERFACE_CC_STALE_IF_ERROR`         | When cache control for the web interface is enabled, controls this [cache-control header attribute](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control). Not set by default. Can be a duration like `1h` (one hour). Qualifier can be `s = second`, `m = minute`, `h = hour` prefixed with a positive number.                                                                                    |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `WEB_INTERFACE_CC_STALE_WHILE_REVALIDATE` | When cache control for the web interface is enabled, controls this [cache-control header attribute](https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control). Not set by default. Can be a duration like `1h` (one hour). Qualifier can be `s = second`, `m = minute`, `h = hour` prefixed with a positive number.                                                                                    |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `WEB_INTERFACE_DARK_THEME_ENABLED`        | If dark mode on web interface is enabled. Defaults to `false`                                                                                                                                                                                                                                                                                                                                                                   |
| `WEB_INTERFACE_ENABLED`                   | If web interface should be enabled.                                                                                                                                                                                                                                                                                                                                                                                             |
| `WEB_INTERFACE_FOOTER_ENABLED`            | If footer on web interface is enabled. Defaults to `true`                                                                                                                                                                                                                                                                                                                                                                       |
| `WEB_INTERFACE_TITLE`                     | If web interface enabled, determines web interface's page. `upda`                                                                                                                                                                                                                                                                                                                                                               |
| `WEBHOOKS_TOKEN_LENGTH`                   | The length of the token. Defaults to `30`, positive number                                                                                                                                                                                                                                                                                                                                                                      |

## Database migration

By default, _upda_ runs database migrations (`DB_MIGRATION_ENABLED`) to create the necessary database schema it uses.
You can disable this behavior by setting it to `false` which might be useful if you have multiple instances running and
only one should (regularly) update the database schema. Please make sure that this instance has the highest priority
when updating _upda,_ and it should also start before all other (updated) instances start. If you have multiple instance
and keep the default that all instances run migrations, some instances might fail to start as they apply locking when
migrating database schema and conflict with other instances while migration is being executed.